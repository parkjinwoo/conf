# layouts/4.conf — 2x2 그리드 레이아웃 (총 4 panes)
# alias 예시:
#   alias tmx-4='tmux new-session \; source-file ~/.config/tmux/layouts/4.conf'

run-shell 'for i in $(seq 1 3); do tmux split-window -t "#{window_id}" -c "#{pane_current_path}"; done'
select-layout tiled
select-pane -t 1 -T "1,1"
