# layouts/9.conf — 3x3 그리드 레이아웃 (총 9 panes)
# alias 예시:
#   alias tmx-9='tmux new-session \; source-file ~/.config/tmux/layouts/9.conf'

run-shell 'for i in $(seq 1 8); do tmux split-window -t "#{window_id}" -c "#{pane_current_path}"; done'
select-layout tiled
select-pane -t 1 -T "1,1"
